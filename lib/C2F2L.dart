import 'package:flutter/material.dart';

class C2F2LWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('hello', style: TextStyle(fontSize: 24)),
    );
  }
} 